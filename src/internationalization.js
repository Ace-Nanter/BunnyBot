/**
 * Manage languages
 */

var getMessage = function(msg, language) {
    
    // If language found or 
        // If message found
            // return message in language
        // Else (if arg language hasn't been defined)
            // Return error message in English

    // Else
        // If message found in English
            // Return message in English
        // Else
            // Error Message

}


exports.getMessage = getMessage;